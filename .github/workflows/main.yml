name: Example Workflow
on:
  push:
    branches: [ developer ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: approvers
      uses: trstringer/manual-approval@v1
      with:
        secret: ${{ github.TOKEN }}
        approvers: kagupatipavan